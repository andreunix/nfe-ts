/**
 * Tipos de retenções.
 *
 * Mantém os tributos retidos separados dos totais normais do documento para
 * preservar a leitura fiscal do XML.
 */
export type { GCredData, RetTribData } from "./index.ts";

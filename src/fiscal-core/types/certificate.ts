/**
 * Tipos de certificado digital.
 *
 * Mantém separados os dados sensíveis carregados em memória e os metadados
 * extraídos do certificado, como na implementação Rust.
 */
export type { AccessKeyParams, CertificateData, CertificateInfo } from "./index.ts";

/**
 * Tipos usados na construção completa do XML.
 *
 * Estes contratos representam a entrada agregada do builder e o resultado final
 * com XML e chave de acesso.
 */
export type { AccessKeyParams, AuthorizedXml, InvoiceBuildData, InvoiceXmlResult } from "./index.ts";

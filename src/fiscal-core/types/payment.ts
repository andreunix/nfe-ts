/**
 * Tipos de pagamento.
 *
 * Separa o meio de pagamento e os detalhes de cartão usados no grupo `pag` da
 * NF-e/NFC-e.
 */
export type { PaymentCardDetail, PaymentData, ReferenceDoc } from "./index.ts";

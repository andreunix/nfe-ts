import type { FiscalRecord } from "../core.ts";

/** Grupo monofásico padrão IBS/CBS. */
export type GMonoPadraoData = FiscalRecord;
/** Grupo monofásico com retenção IBS/CBS. */
export type GMonoRetenData = FiscalRecord;
/** Grupo monofásico retido anteriormente. */
export type GMonoRetData = FiscalRecord;
/** Grupo de diferimento monofásico. */
export type GMonoDifData = FiscalRecord;
/** Grupo agregado monofásico IBS/CBS. */
export type GIbsCbsMonoData = FiscalRecord;
